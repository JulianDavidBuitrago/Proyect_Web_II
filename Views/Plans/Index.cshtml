@model IEnumerable<ZOOLO.Models.Plans>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Zoologico.NombreZoo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ZonaPlans)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorPlans)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ZonaAdicPlans)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Zoologico.NombreZoo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ZonaPlans)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorPlans)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ZonaAdicPlans)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPlans }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPlans }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPlans })
        </td>
    </tr>
}

</table>
