@model IEnumerable<ZOO_PROYECT.Models.Parqueadreo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TelefonoPar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorHora)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Zoologico.NombreZoo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TelefonoPar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorHora)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Zoologico.NombreZoo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPar }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPar }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPar })
        </td>
    </tr>
}

</table>
